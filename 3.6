#include <stdio.h>
//스와핑을 이용한 포인터 스와핑
 

void swap(int* px, int* py)

{

	int tmp;

	tmp = *px;

	*px = *py;

	*py = tmp;

}

 

int main(void) {

	int a = 1, b = 2;

	printf("swap을 호출하기 전: a=%d, b=%d\n", a, b);

	swap(&a, &b);

	printf("swap을 호출한 후 : a=%d, b=%d\n", a, b);

	return 0;

}
